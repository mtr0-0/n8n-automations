Thanks for contributing!

- Add exported workflow JSON files into workflows/
- Include a one-line description and required credentials in the README or create a new markdown file describing the workflow.
- Do NOT include secrets or credentials in commits. Use placeholders if needed.
- Open a PR to propose changes.
